<!-- DashboardView.xaml -->
<UserControl x:Class="AI_Crypto_Signal_Pro.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource PrimaryBackgroundBrush}"
             TextElement.Foreground="{DynamicResource PrimaryTextBrush}"
             TextElement.FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Stat Cards -->
        <Grid Grid.Row="0" Height="120" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" CornerRadius="10" Background="{DynamicResource CardBackgroundBrush}" Margin="0,0,10,0" Padding="16">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Total Signals" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                    <TextBlock Text="{Binding TotalSignals}" FontSize="24" FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
            <Border Grid.Column="1" CornerRadius="10" Background="{DynamicResource CardBackgroundBrush}" Margin="0,0,10,0" Padding="16">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Win Rate" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                    <TextBlock Text="{Binding WinRate, StringFormat={}{0:F1}%}" FontSize="24" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" CornerRadius="10" Background="{DynamicResource CardBackgroundBrush}" Margin="0,0,10,0" Padding="16">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Active Trades" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                    <TextBlock Text="{Binding ActiveSignals}" FontSize="24" FontWeight="Bold"
                               Foreground="{DynamicResource InfoBrush}" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
            <Border Grid.Column="3" CornerRadius="10" Background="{DynamicResource CardBackgroundBrush}" Padding="16">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="AI Confidence" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                    <TextBlock Text="{Binding AvgConfidence, StringFormat={}{0:F0}}" FontSize="24" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Recent Signals -->
        <TextBlock Grid.Row="1" Text="Recent Signals" FontWeight="SemiBold" FontSize="16"
                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12" />

        <Border Grid.Row="2" CornerRadius="10" Background="{DynamicResource CardBackgroundBrush}" Padding="16">
            <DataGrid ItemsSource="{Binding RecentSignals}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      BorderThickness="0"
                      Background="Transparent"
                      RowHeight="40"
                      HeadersVisibility="Column"
                      >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Coin" Binding="{Binding Coin}" Width="*" />
                    <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="*" />
                    <DataGridTextColumn Header="Entry" Binding="{Binding Entry, StringFormat=N4}" Width="*" />
                    <DataGridTextColumn Header="SL" Binding="{Binding StopLoss, StringFormat=N4}" Width="*" />
                    <DataGridTextColumn Header="TP1" Binding="{Binding TakeProfit, StringFormat=N4}" Width="*" />
                    <DataGridTextColumn Header="Conf" Binding="{Binding Confidence}" Width="*" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>





