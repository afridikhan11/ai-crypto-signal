<!-- ========== Views/HistoryView.xaml ========== -->
<UserControl x:Class="AI_Crypto_Signal_Pro.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource PrimaryBackgroundBrush}"
             TextElement.Foreground="{DynamicResource PrimaryTextBrush}"
             TextElement.FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Trade History" FontSize="22" FontWeight="SemiBold" />
            <TextBlock Text="Review all executed and closed trades" FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0" />
            <TextBlock Text="{Binding ErrorMessage}" FontSize="12" Foreground="#FF5252"
                       Margin="0,4,0,0" />
        </StackPanel>

        <Border Grid.Row="1" CornerRadius="10" Background="{DynamicResource CardBackgroundBrush}"
                Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Margin="0,0,12,0"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Search coin..."
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <ComboBox Grid.Column="1" Margin="0,0,12,0"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              materialDesign:HintAssist.Hint="Date Range"
                              ItemsSource="{Binding DateFilters}"
                              SelectedItem="{Binding SelectedDateFilter}" />
                    <ComboBox Grid.Column="2" Margin="0,0,12,0"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              materialDesign:HintAssist.Hint="Status"
                              ItemsSource="{Binding StatusFilters}"
                              SelectedItem="{Binding SelectedStatusFilter}" />
                    <Button Grid.Column="3" Margin="0,0,12,0"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Content="{materialDesign:PackIcon Kind=Filter}"
                            Width="40" Height="40"
                            Command="{Binding ApplyFilterCommand}" />
                    <Button Grid.Column="4" Margin="0,0,12,0"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Content="{materialDesign:PackIcon Kind=Refresh}"
                            Width="40" Height="40"
                            Command="{Binding RefreshCommand}" />
                    <Button Grid.Column="5"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Content="{materialDesign:PackIcon Kind=FileExcel}"
                            Width="40" Height="40"
                            Command="{Binding ExportCommand}" />
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" CornerRadius="10" Background="{DynamicResource CardBackgroundBrush}"
                Padding="16">
            <DataGrid ItemsSource="{Binding Trades}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      BorderThickness="0"
                      Background="Transparent"
                      RowHeight="40"
                      HeadersVisibility="Column"
                      SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=yyyy-MM-dd HH:mm}" Width="140" />
                    <DataGridTextColumn Header="Coin" Binding="{Binding Coin}" Width="*" />
                    <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="*" />
                    <DataGridTextColumn Header="Entry" Binding="{Binding EntryPrice, StringFormat=N8}" Width="*" />
                    <DataGridTextColumn Header="Stop Loss" Binding="{Binding StopLoss, StringFormat=N8}" Width="*" />
                    <DataGridTextColumn Header="TP" Binding="{Binding TakeProfit, StringFormat=N8}" Width="*" />
                    <DataGridTextColumn Header="Conf" Binding="{Binding Confidence}" Width="Auto" />
                    <DataGridTemplateColumn Header="Result" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ResultLabel}" Foreground="{Binding ResultColor}"
                                           FontWeight="SemiBold" VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>